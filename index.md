---
layout: home
title: Home
---

# Bitcoin Halving-Cycle Framework

A long-term research framework that studies Bitcoin's halving cycles and their
cross-asset correlations with **ETH, SOL, XRP** (altcoins) and **SPX, NDX, DXY,
TLT** (global macro). The goal is to forecast the **time ranges and
entry/exit zones of the next BTC cycle** from a holistic perspective -- *not*
to build an automated trading bot.

This page is the **single-page narrative** of the white paper. Use the sidebar
on the left to jump to any section, or simply scroll. The page is split into
two parts: a short **Decision** spine (the load-bearing actionable headline)
followed by **Appendices** (technical depth); every chart is paired with an
explicit *model input / decision overlay / context* role label so the reader
knows whether a given exhibit moves a published number or just describes
the environment.

## What this page covers

**Decision spine** (read first — the actionable headline):

- **[Abstract](#abstract)** -- thesis, non-goals, source-of-truth files, and
  a guide to the three role labels (`model input` / `decision overlay` /
  `context`) used throughout the document.
- **[The Prediction (BTC)](#predictive-ranges)** -- the load-bearing
  **decision windows** for the next BTC cycle (B4 / accumulation /
  distribution / exit), 2-stage projection chain, and folklore cross-check.
- **[Per-Asset Decision Windows](#cross-asset-timing)** -- ETH / XRP / SOL
  halving-calendar attention windows in BTC-B4 calendar order, plus the
  macro context for SPX / NDX / DXY / TLT (post-v1 extension I-17).
- **[Confidence & Limits](#validation)** -- LOOCO backtest, macro-regime
  robustness, honest caveats as a confidence discount on the spine.

**Appendices** (read when you want the math / the exhibits):

- **[A. Methodology](#methodology)** -- Rule T/B extrema, halving-day
  alignment, phase-conditioned correlation, forward ranges, validation;
  each method tagged with its role. Includes the 2-stage projection fit
  equations and the cross-check formula that drive [The Prediction](#predictive-ranges).
- **[B. Cycle Anatomy](#cycle-anatomy)** -- Bitcoin's four halving-to-halving
  cycles, the SMA valuation floor overlay (decision overlay, not a model
  input), the Cowen-memo reconciliation, and the folklore bull-rhythm
  cross-check.
- **[C. Cross-Asset Correlations](#cross-asset)** -- BTC's phase-conditioned
  correlation with ETH, SOL, XRP and SPX/NDX/DXY/TLT. (*Context, not
  forecast* — describes the environment the bands live in.)
- **[D. Release Checklist](#release-checklist)** -- the 16-item manual
  release checklist defined in `DESIGN.md` §10.2.
- **[E. Theoretical Framework](#theory)** -- econophysics cross-check
  (V1-V4 + four predictive-gate failures + honest mechanism-open caveat).
  *Context, not forecast* -- describes why the cycle's structural coherence
  holds while amplitude compresses; does not move B4 / C5.
- **[Status](#status)** -- increment-by-increment gate results.

---
